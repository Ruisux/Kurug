__pycache__/
*.pyc
.venv/
.env
*.db

# Avatares subidos por usuarios (contenido, no código)
static/avatars/*
!static/avatars/.gitkeep

# Historial en disco (pizarras, etc.) — datos locales del server
data/
