# Hosts extra que Vite acepta en dev (además de localhost), separados por comas.
# Copia este archivo a .env.local y pon la IP LAN / hostname de TU máquina.
# git ignora .env.local (patrón *.local), así cada equipo tiene el suyo.
KURUG_ALLOWED_HOSTS=192.168.1.50,mi-equipo.local
